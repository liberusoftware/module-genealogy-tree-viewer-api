# Genealogy TreeViewer api

This package is the one-to-one **api** presentation adapter for `liberusoftware/module-genealogy-tree-viewer`.

It owns only api transport/presentation integration. Domain rules, persistence, authorization, tenancy, and lifecycle behavior remain in the matching core package.

- Composer package: `liberusoftware/module-genealogy-tree-viewer-api`
- Installer name: `genealogy-tree-viewer-api`
- Package type: `liberu-module`

